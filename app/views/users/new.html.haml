- provide(:title, 'Sign up')
%h1 Sign up
.row
  .col-md-6.col-md-offset-3
    = form_for(@user) do |f|
      = render 'form', f: f, user: @user
      .form-actions
        = f.submit 'Crear alumno', class: 'btn btn-primary'
= link_to 'Back', users_path
